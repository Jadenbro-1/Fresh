#!/bin/bash

# Navigate to the node_modules directory
cd node_modules/react-native-gesture-handler

# Rename the podspec file
mv RNGestureHandler.podspec react-native-gesture-handler.podspec

echo "Podspec file renamed to react-native-gesture-handler.podspec"
